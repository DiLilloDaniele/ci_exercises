name: Daniele Workflow
on:
  push:
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Preparing
        uses: joschi/setup-jdk@v2.3.0
        with:
          java-version: 17
      - name: Build
        run: ./gradlew HelloDaniele